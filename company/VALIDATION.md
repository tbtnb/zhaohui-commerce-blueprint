# 新版公司方案验证

日期：2026-09-15。目标：本机静态网站 `http://127.0.0.1:8765/`。

## 实际执行

- 新版构建成功；所有 `company/` JavaScript 模块语法检查通过；差异空白检查通过。
- `node --test company/model.test.mjs`：4 项检查通过。覆盖 270 组目标/经营方式/团队/细分组合、八个主方案和 24 个细分方向的独立内容、资料缺失、文件状态、导出、文本转义及关键业务口径。
- 通过 Codex 浏览器实际点击八个完整流程，从首页到最终结果。诊断 16 屏，其余 17 屏；每条观看八段内容。四条使用稍后补充，四条使用文件；另跑多渠道已有连接和供货引导接入。
- 在供货流程实际测试空渠道拦截、切到另一账号后拒绝继续、读取失败后重试、三份资料核对、异常暂停与人工核实恢复。
- 播放器逐步前进、重播、速度切换与自动播放至结尾可用；减少动效入口可用。
- 最终页重复资料反馈显示沿用原任务；新资料处理逻辑保留人工确认边界。
- 返回经营方式改为会员后，旧结果页被移除，重新进入团队问题。
- 浏览器触发搭建说明下载，实际文件在 Downloads，读取确认主方案、实施缺口和不启动任务说明。浏览器下载事件监听超时，但磁盘文件证明确已下载；不将事件监听标为通过。
- 桌面默认视口和 390×844 手机视口实际检查。手机供货动画页无横向溢出，按钮和表单可操作；测试结束恢复默认视口。
- 研究页已在浏览器打开，八套规格、细分表格、能力快照及 18 条来源/访问记录可见。控制台错误记录为空。

## 范围

八条完整路线的细分选择不同；24 个细分方向的全部内容由纯模型检查覆盖，未声称 24 条都完成浏览器逐帧人工验收。没有真实平台接入、业务发送、ROI 验证、线上发布或真实商户访谈。旧场景检查不能当作本版验收。

## 重现主路径

首页选“团队能不能少做些重复工作” → 多个渠道一起经营 → 6–30 人 → 勾选渠道 → 选择现有系统 → 确认公司 → 选择经营重点 → 逐项核对示例资料 → 诊断 → 多渠道方案 → 工作说明 → 三步搭建 → 播放八段过程 → 结果与下载。
