# 场景优先改版

目标：把首页改为中国国内电商的60个场景、7套可组合方案和可导出的经营任务说明。原技术报告保留为附录。
范围：只修改公开报告仓库；不修改 online-agent 业务代码、不连接真实店铺、不创建运行任务、不执行财务或外部业务写入。
每个场景：用户原话、具体情境、所需资料、发现方式、可交付产物、反馈改进、人工边界、观察指标及证据。场景是设计，不是60个已安装功能。
现有能力边界：Timer/工作区状态可复用；通用 Listener 仅 workspace_files；FDE 通用连接器 data.read；客服 shadow/copilot；新平台读取和逐场景运行仍需验证。
网页组合器：仅本地选择与导出。格式为 planning brief，不是当前 App 可导入的运行配置；不会创建连接、注册 Timer 或授予权限。
自我迭代：自动记录观察与产出候选，按人工反馈比较；正式政策/身份映射/对外话术/生产连接器变更需确认和业务验收。无新证据时不自造规则。
验证：60场景完整性、过滤/深链接、组合/移除/预设、导出内容、键盘/无JS阅读、移动端、无外部请求、线上回归。
